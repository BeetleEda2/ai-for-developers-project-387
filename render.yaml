services:
  - type: web
    name: call-booking
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: frankfurt
    healthCheckPath: /owner
    envVars:
      - key: PORT
        value: 10000
